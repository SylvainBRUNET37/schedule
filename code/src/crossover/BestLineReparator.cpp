#include "../../headers/crossover/BestLineReparator.h"

void BestLineReparator::execute(Solution* firstChild, Solution* secondChild) {
	vector<int> temp;
	SolutionValidator;
	int nbColumns = firstChild->v_v_IdShift_Par_Personne_et_Jour[0].size();
	int nbLignes = firstChild->v_v_IdShift_Par_Personne_et_Jour.size();
	for (int i = 0; i < nbLignes; i++) {
		//if(firstChild[i].)
	}
}
